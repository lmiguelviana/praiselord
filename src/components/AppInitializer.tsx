import { useEffect } from 'react';

const AppInitializer = () => {
  useEffect(() => {
    // Inicialização da aplicação pode ser feita aqui
    // Espaço reservado para futuras inicializações, 
    // como preparação de dados, verificações, etc.
  }, []);

  return null;
};

export default AppInitializer; 